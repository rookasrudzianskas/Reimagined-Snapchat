import React from 'react';

const videoConstrains = {
    width: 250,
    height: 400,
    facingMode: "user",
};

const WebcamCapture = () => {
    return (
        <div className="webcamCapture">

        </div>
    );
};

export default WebcamCapture;
